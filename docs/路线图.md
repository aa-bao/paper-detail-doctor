# 路线图：先 A 打通，再 B 分批升级

原则（2026-09-17 拍板）：**A 阶段原样搬运 + 把路径/规则外置成配置，不做重写；B 阶段按价值分批升级为参数化 CLI + yaml 规则外置。**

每个批次交付一个**可独立使用的子 skill**，不是散装脚本 —— 这样任何阶段中断，包里已有的子 skill 都能单独用。

---

## A 阶段 · 打通可用

| 批次 | 交付的子 skill / 组件 | 状态 |
|---|---|---|
| **A1** | 包骨架 + git 仓库 + 根 `SKILL.md`（索引与路由）+ `shared/contracts/`（Issue / PlanItem schema）+ 目录重构为多 skill 包 | ✅ 完成 |
| **A2** | `skills/template-extract` —— `paper-template-extractor` 迁入，并扩展**体例规则**提取（编码制、引注形态与位置、题注位置与格式、页眉、页码分节、摘要上限） | ✅ 完成 |
| **A3** | `shared/lib/docx_scan.py` + `report.py` + `docx_ops.py` + `ooxml_guard.py`（读侧 / 写侧 / 三道闸门） | ✅ 完成 |
| **A4** | `skills/cite-doctor`（L2 全部 7 项：审计 + 手术）+ `workflow/{audit_all,plan,apply}.py` + 三份验收测试 | ✅ 完成 |
| **A5** | `skills/format-audit`（L1）+ `skills/text-style`（L3）+ `skills/structure-length`（L4 的 22–25） | ⬜ |
| **A6** | `skills/proposal-consistency`（L4-26）+ `tests/golden/` 金标准跑分 | ⬜ |
| **A7** | 各子 skill 的 `SKILL.md` 定稿 + `references/` SOP + 安装到 `~/.workbuddy/skills/` | ⬜ |

**A1 为什么把目录重构成 `skills/` 子包**：早期骨架把全部代码放在包根的 `scripts/` 里，等于把整套做成**一个** skill。你要的是**包**（可容纳多个 skill），所以包根只留索引与编排，检查逻辑全部下沉到 `skills/<子 skill>/`。

---

## B 阶段 · 分批升级

优先升 B（最通用、复用率最高）：

1. **`cite-doctor`（L2 引注）** —— 三级优先的体例解析、多种编码制支持
2. **`template-extract`** —— 体例规则的自动识别（从范文反推规则，而非人工填）
3. **`proposal-consistency`** —— 锚点抽取与 L2 语义判定的稳定性

可以长期留在 A 的：

- 项目专属的一次性替换映射（标题平实化等）—— 走 `assets/rules/*.yaml`，不写死在代码里

---

## 验收标准（封装完成的定义）

必须能对**一篇与本项目完全无关的论文**跑通全链路，且**不改任何脚本代码，只改配置**：

1. 指定 docx（或 md 目录）+ 参考文献表 → 一键渲染引注 / 建立跳转
2. `workflow/audit_all.py` 出报告 → 每条带定位、期望/实际对照、标准来源
3. `workflow/plan.py` 出可编辑计划 → `apply.py` 执行 → 幂等复跑结果一致
4. 字数统计报出双口径 + 分章配额差
5. 页眉/目录/PDF 导出链路可用
6. 金标准跑分出三指标数字（召回 / 误报 / 定位准确率）
7. 单点请求能被根 `SKILL.md` 正确路由到对应子 skill，且**不触发其他子 skill**

---

## 记录

- **2026-09-17 A1**：包目录定为 `F:\Coding\Project\paper-detail-doctor`；git 初始化（`f3f78fd`）；设计方案 v1/v2 + 本路线图入库。
- **2026-09-17 A1 修订**：澄清为多 skill 包后重构 —— 新增根 `SKILL.md`（索引 + 编排）、`skills/` 下 7 个子 skill 目录、`shared/{lib,contracts,references}/`、`workflow/`；原包根 `scripts/` 与 `optional/` 移除（`quote_verify` 升格为 `skills/quote-verify`）。
- **2026-09-17 A2**：`paper-template-extractor` 迁入 `skills/template-extract/`（9 个脚本 + 3 份 references + spec 示例）；新增 `scripts/extract_conventions.py` 反推六组体例规则；`references/format_spec_schema.md` 增补 `conventions` 块定义与三条使用纪律。
  同时新增 **`assets/skills-index.yaml`**（路由唯一真源）与根 `SKILL.md` 的**六步路由程序**（含无参打印目录、歧义让用户选、无匹配不硬凑、先声明再执行、写盘边界）。
  验证：在真实论文 docx 上跑通 `extract_conventions.py`，8 项真值命中 7 项（第 8 项 `multiple` 是测试期望值写错，非脚本问题）。
- **2026-09-17 A3**：共享层落定。
  · 读侧 `docx_scan.py`：自写段落遍历（`w:hyperlink` 与域结果都算进可见文本，python-docx 的 `p.text` 会漏掉引注）；引注位置按"后一字符是句读符→之前 / 前一字符是分隔符→之后 / 两侧皆正文→句中"三判；参考文献表扫描用归一化标题（吃「摘　　要」这种全角空格）防越界。
  · 写侧 `docx_ops.py`：12 个具名动作 + `Locator` 三元组指纹定位（段号只是提示，指纹硬校验，失配抛 `LocateError` 而不就近猜）；引注按**整块**移动（域/链接一并带走）。
  · 闸门 `ooxml_guard.py`：快照（保留 12 份、同秒重号防覆盖、按文件名时间戳排序）/ 幂等账本（键含文档哈希）/ 受控执行（先立书签→再原地改→最后结构性动作按段号倒序；单条失败不打断整批）/ 原子保存带回退。
  · 验收 `tests/selftest_shared_lib.py` 36 项。**测试当场揪出 6 个真 bug**：
    ① `field_code_of_run` 反向扫描把 `separate` 也算嵌套深度 → 永远找不到配对 `begin` → 拆链接静默失效；
    ② `strip_field_hyperlink` 把域结果 run 一并删除 → 等于抹掉正文里的 `[12]`；
    ③ `para.insert_after` 漏进 `STRUCTURAL_ACTIONS` → 段落表不刷新，后续动作拿过期段号定位；
    ④ `merge_paragraph_into` 无脑 append → 合并后语序颠倒；
    ⑤ `SnapshotManager.restore` 先备份后读源，源快照被 `_prune` 裁掉 → 回滚直接失效；
    ⑥ `pBdr` 子元素顺序未按 schema 约束插入 → Word/WPS 可能报错。
- **2026-09-17 A4**：`cite-doctor` + workflow 落定，并建立本包的**验收范式**。
  · 七条规则：`cite-position`（引注须在句读符之前，跟在其后算违规）/ `cite-form`（上标+方括号，**且不得显式写 `w:sz`** —— 上标本身会被渲染器缩到约 65%，再写死字号等于缩两次）/ `cite-jump`（正文锚点缺失·悬空·错指，文献表 `refN` 书签缺失，双向都查）/ `cite-numbering`（编号连续、顺序编码制首现序）/ `ref-coverage`（正反覆盖）/ `ref-format`（GB/T 7714 可纯文本判定的项）/ `ref-duplicate`。
  · workflow：`audit_all.py`（子进程隔离跑子 skill，一条崩了不拖垮其余）→ `plan.py`（生成带注释的勾选表，保守默认勾选）→ `apply.py`（预览 + 确认 + 受控执行 + 回滚入口）。
  · **验收范式（后续每个子 skill 都要照此配一份）**：注入式测试 —— 在一份**干净稿**上人为造出每一种缺陷，确认检查器抓得到；再跑自动修复，确认问题消失且幂等。理由写在 `tests/selftest_cite_doctor.py` 开头：一个永远返回空列表的函数也能在干净稿上"零问题"。
  · 两次真实验证：干净稿上只剩 1 条真问题（文献 [20] 缺页码，与本项目原本的待办吻合）；注入 8 类缺陷全部检出并可修复。`tests/selftest_cite_doctor.py` 37 项、`tests/e2e_cli.py` 37 项全绿。
  · 本轮测试揪出的 bug 里有 **3 个属于"静默失效"类**（`ctx.act()` 参数与 `name` 撞名被单规则 try/except 吞掉、位置判定漏 `；，` 之后的引注、旧 plan 失效时报"什么都没做"而非报错）。已据此加两条硬约束：① 规则异常必须升格为 `audit-internal-error` 的 error 级 Issue，不许只写进 notes（否则"检查器坏了"会伪装成"全部通过"）；② `apply` 的顺序固定为**先哈希门禁、后回填参数**，过期 plan 一律报错退出。
